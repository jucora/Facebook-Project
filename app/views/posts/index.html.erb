<h1>Posts</h1>
<% @posts.each do |p| %>
  <p>Author: <%= User.find(p.user_id).name %></p>
  <p>Post: </p>
  <%= p.body %>
  <br>
  <br>
  <%= link_to "update", edit_post_path(p) %>
  <%= link_to "delete", p, method: :delete, data: { confirm: "Are you sure?" } %>
  <hr>
<% end %>
